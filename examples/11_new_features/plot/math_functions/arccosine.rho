include plot
println("=== arccosine ===")
fun f(float64: x) -> float64 { return math.acos(x) }
figure(800, 510)
plot(f, -1, 1, "f(x)", "blue")
title("arccosine")
xlabel("x")
ylabel("f(x)")
legend(1)
int: bytes = savefig("arccosine.svg")
println("Wrote ", bytes, " bytes to arccosine.svg")
