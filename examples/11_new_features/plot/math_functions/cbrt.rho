include plot
println("=== cbrt ===")
fun f(float64: x) -> float64 { return math.cbrt(x) }
figure(800, 510)
plot(f, -8, 8, "f(x)", "blue")
title("cbrt")
xlabel("x")
ylabel("f(x)")
legend(1)
int: bytes = savefig("cbrt.svg")
println("Wrote ", bytes, " bytes to cbrt.svg")
