include plot
println("=== relu ===")
fun f(float64: x) -> float64 { return x > 0 ? x : 0 }
figure(800, 510)
plot(f, -3, 3, "f(x)", "blue")
title("relu")
xlabel("x")
ylabel("f(x)")
legend(1)
int: bytes = savefig("relu.svg")
println("Wrote ", bytes, " bytes to relu.svg")
