include plot
println("=== cosecant ===")
fun f(float64: x) -> float64 { return 1 / math.sin(x) }
figure(800, 510)
plot(f, 0.2, 3.0, "f(x)", "blue")
title("cosecant")
xlabel("x")
ylabel("f(x)")
legend(1)
int: bytes = savefig("cosecant.svg")
println("Wrote ", bytes, " bytes to cosecant.svg")
