include plot
println("=== chirp ===")
fun f(float64: x) -> float64 { return math.sin(x * x) }
figure(800, 510)
plot(f, -5, 5, "f(x)", "blue")
title("chirp")
xlabel("x")
ylabel("f(x)")
legend(1)
int: bytes = savefig("chirp.svg")
println("Wrote ", bytes, " bytes to chirp.svg")
