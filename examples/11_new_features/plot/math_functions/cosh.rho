include plot
println("=== cosh ===")
fun f(float64: x) -> float64 { return math.cosh(x) }
figure(800, 510)
plot(f, -2, 2, "f(x)", "blue")
title("cosh")
xlabel("x")
ylabel("f(x)")
legend(1)
int: bytes = savefig("cosh.svg")
println("Wrote ", bytes, " bytes to cosh.svg")
