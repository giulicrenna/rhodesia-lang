include plot
println("=== softplus ===")
fun f(float64: x) -> float64 { return math.log(1 + math.exp(x)) }
figure(800, 510)
plot(f, -3, 3, "f(x)", "blue")
title("softplus")
xlabel("x")
ylabel("f(x)")
legend(1)
int: bytes = savefig("softplus.svg")
println("Wrote ", bytes, " bytes to softplus.svg")
