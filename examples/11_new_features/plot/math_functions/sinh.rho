include plot
println("=== sinh ===")
fun f(float64: x) -> float64 { return math.sinh(x) }
figure(800, 510)
plot(f, -2, 2, "f(x)", "blue")
title("sinh")
xlabel("x")
ylabel("f(x)")
legend(1)
int: bytes = savefig("sinh.svg")
println("Wrote ", bytes, " bytes to sinh.svg")
