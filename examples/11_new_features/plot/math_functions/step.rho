include plot
println("=== step ===")
fun f(float64: x) -> float64 { return  x >= 0 ? 1 : 0 }
figure(800, 510)
plot(f, -3, 3, "f(x)", "blue")
title("step")
xlabel("x")
ylabel("f(x)")
legend(1)
int: bytes = savefig("step.svg")
println("Wrote ", bytes, " bytes to step.svg")
